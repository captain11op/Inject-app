# Inject-app
It can inject text in images which can be seen by anyone whos having that image and this app


# How to use

Make a parent folder inside that put hte main.py file inside the parent folder make folder name images ( name should be same) inside that putt all the images 

# Modules used
Tkinter
PIL
OS
stegano
